<x-customer-layout>
    <div class="container">
        <ul class="flex flex-col space-y-4">
            {{-- @if ($categories)
            @foreach ($categories as $c)    --}}
            <li>
                <a href="#" class="text-white">tes</a>
            </li>
            <h1>wo</h1>
            {{-- @endforeach
            @endif --}}
        </ul>
    </div>
</x-customer-layout>